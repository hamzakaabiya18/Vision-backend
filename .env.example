MONGO_URI=
JWT_SECRET=
PORT=5000
CLIENT_URL=http://localhost:3000

# Optional — support bot email notifications. If unset, support messages
# are still saved to the database, just no email is sent.
SUPPORT_EMAIL=
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
FROM_EMAIL=

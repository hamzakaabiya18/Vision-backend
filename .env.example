MONGO_URI=
JWT_SECRET=
PORT=5000
CLIENT_URL=http://localhost:3000

# Optional — bot/admin direct-message email notifications. If unset,
# messages are still saved to the database, just no email is sent.
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
FROM_EMAIL=

# Fallback recipient if a persona-specific address below isn't set.
SUPPORT_EMAIL=
# Per-persona recipients for the official VISION bot accounts.
HAMZA_EMAIL=
ASSALE_EMAIL=
MAY_EMAIL=
